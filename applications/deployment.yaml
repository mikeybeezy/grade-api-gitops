apiVersion: apps/v1
kind: Deployment
metadata:
  name: grade-api-api
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grade-api-api
  template:
    metadata:
      labels:
        app: grade-api-api
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: grade-api-api
        image: ghcr.io/mikeybeezy/kubernetes-cicd-pipeline:latest
        ports:
        - containerPort: 3000
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"
          requests:
            memory: "128Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: grade-api-api
  namespace: default
spec:
  selector:
    app: grade-api-api
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
